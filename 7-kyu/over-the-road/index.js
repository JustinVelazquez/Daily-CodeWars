function overTheRoad(address, n) {
  //we take in both the address of our house and the length of each row of houses
  // we want to return a digit which will represent

  return n * 2 + 1 - address;
}
