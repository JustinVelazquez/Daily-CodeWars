function invertedRanges(ranges) {
  // We take in range bound array of 2 positive ints
  // We return the inverse range bounds of our initial range. The ints will not overlap and has a lower bound limit of 0 and upper of 100
  // EX => [[0, 25], [51, 75]]) == [[26, 50], [76, 100]
  // If our initial range covers 0-100 we return an empty array
}
